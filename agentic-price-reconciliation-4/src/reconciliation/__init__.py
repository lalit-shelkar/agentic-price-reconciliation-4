"""Agentic price/barrier break reconciliation.

Two cooperating agents over a durable state machine. Read `requirements.md`, then
`architecture.md`, then `specs/`. Module-to-spec mapping is in OWNERSHIP.md.
"""

__version__ = "0.1.0"
